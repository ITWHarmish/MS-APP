// users

export enum UserRoles {
  ADMIN = 1,
  MANAGEMENT = 2,
  TEAMLEAD = 3,
  STANDARD = 4,
}

// Project

export enum ProjectStatus {
  NOT_STARTED = 0,
  IN_PROGRESS = 1,
  COMPLETE = 2,
  ON_HOLD = 3,
}
